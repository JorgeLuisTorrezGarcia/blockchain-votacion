--- CONFIGURACIÓN DE PUERTO ---

PORT=4000

--- CONFIGURACIÓN DE BASE DE DATOS POSTGRES ---

DB_DIALECT=postgres
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=*******
DB_NAME=voting_app
DB_PORT=5432

--- CONFIGURACIÓN DE SEGURIDAD ---

JWT_SECRET=tu_secreto_muy_largo_y_seguro_para_jwt
JWT_EXPIRATION=1h

--- CONFIGURACIÓN DE ORÍGENES PERMITIDOS ---

# URL del frontend Next.js que consumirá la API. Cambiar en despliegues.
FRONTEND_ORIGIN=http://localhost:3000

--- INTEGRACIÓN CON SMART CONTRACTS ---

# Dirección del contrato VotingSystem desplegado. Debes actualizar este valor luego de cada despliegue.
SMART_CONTRACT_ADDRESS=0x5fbd

# RPC que usará el backend para leer datos on-chain. Por defecto apunta al nodo local de Hardhat/Ganache.
SMART_CONTRACT_RPC_URL=http://127.0.0.1:8545

# Ruta absoluta hacia el ABI del contrato (opcional). Si se deja vacío, se intentará cargar desde ../smart-contracts/artifacts.
# SMART_CONTRACT_ABI_PATH=C:/ruta/completa/al/archivo/VotingSystem.json

--- OPERACIONES ADMIN / FIRMA ON-CHAIN ---

# API Key usada para proteger endpoints administrativos (ej. sincronizar whitelist).
# Genera un string aleatorio y compártelo solo con clientes de confianza.
ADMIN_API_KEY=admin_api_key_123456789

# Private key del deployer/creador de elecciones que firmará authorizeVoters.
# Nunca la compartas y solo úsala en entornos controlados.
ADMIN_WALLET_PRIVATE_KEY=0xac...